[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mlarena"
version = "0.2.0"
description = "Python SDK for ML Arena - submit agents, manage competitions, courses, and view leaderboards"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = [
    "requests>=2.32.0",
]

[project.optional-dependencies]
pandas = ["pandas>=1.0.0"]

[project.urls]
Homepage = "https://ml-arena.com"
Repository = "https://github.com/ml-arena/mlarena-sdk"
